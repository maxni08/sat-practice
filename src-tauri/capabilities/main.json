{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Only the local practice window can access application commands. Desmos has no native permissions.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
